.vercel
.DS_Store
**/xcuserdata/
*.xcuserstate
**/DerivedData/
.swiftpm/

# Gemini / segreti locali (iOS)
02_GIGI_APP/Config.xcconfig

# Contesto dev privato (host MacInCloud, workflow SSH) — non committare
# CLAUDE.md (root) ora è team-shared (committato). Il contesto privato vive in CLAUDE.local.md
/CLAUDE.local.md
bug/
*.gigi-secret.txt

# Hooks runtime / Claude Code
.playwright-mcp/
.claude/settings.local.json
# Per-instance build hooks (template committato come local-build.sh.example)
.claude/local-build.sh
# Working files della routine PR review (checklist correnti)
review-checklists/
!review-checklists/.gitkeep

# Codex agents config — committed selectively (agents/skills/prompts only)
.codex/*
!.codex/agents/
!.codex/agents/**
!.codex/skills/
!.codex/skills/**
.codex/skills/.system/**
!.codex/prompts/
!.codex/prompts/**

# .omx is the deep-interview workspace; only interviews/ + specs/ are committed
.omx/context/
.omx/scratch/

# Xcode build artifacts
02_GIGI_APP/build/
02_GIGI_APP/buildServer.json

# Scratch / debug local notes
debug console xcode.md
voice.md
__pycache__/
