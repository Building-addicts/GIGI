name: Discord Notify

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened, labeled, closed]
  pull_request:
    types: [opened, closed]

permissions:
  issues: read
  pull-requests: read

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Issue comment on LIVE FEED 19
        if: github.event_name == 'issue_comment' && github.event.issue.number == 19
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          AUTHOR: ${{ github.event.comment.user.login }}
          BODY: ${{ github.event.comment.body }}
          URL: ${{ github.event.comment.html_url }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          SHORT=$(printf '%s' "$BODY" | head -c 1500)
          CONTENT=$(printf '📊 **LIVE FEED** — @%s\n%s\n\n[apri commento](%s)' "$AUTHOR" "$SHORT" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"

      - name: Bug issue opened
        if: github.event_name == 'issues' && github.event.action == 'opened' && contains(github.event.issue.labels.*.name, 'bug')
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NUM: ${{ github.event.issue.number }}
          TITLE: ${{ github.event.issue.title }}
          URL: ${{ github.event.issue.html_url }}
          AUTHOR: ${{ github.event.issue.user.login }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          CONTENT=$(printf '🐛 **BUG APERTO** — #%s\n**%s**\nby @%s\n%s' "$NUM" "$TITLE" "$AUTHOR" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"

      - name: Release blocker labeled
        if: github.event_name == 'issues' && github.event.action == 'labeled' && github.event.label.name == 'release-blocker'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NUM: ${{ github.event.issue.number }}
          TITLE: ${{ github.event.issue.title }}
          URL: ${{ github.event.issue.html_url }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          CONTENT=$(printf '🚨 **RELEASE-BLOCKER** — #%s\n**%s**\n%s' "$NUM" "$TITLE" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"

      - name: PR opened
        if: github.event_name == 'pull_request' && github.event.action == 'opened'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NUM: ${{ github.event.pull_request.number }}
          TITLE: ${{ github.event.pull_request.title }}
          URL: ${{ github.event.pull_request.html_url }}
          AUTHOR: ${{ github.event.pull_request.user.login }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          CONTENT=$(printf '📤 **PR APERTO** — #%s\n**%s**\nby @%s\n%s' "$NUM" "$TITLE" "$AUTHOR" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"

      - name: PR merged
        if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NUM: ${{ github.event.pull_request.number }}
          TITLE: ${{ github.event.pull_request.title }}
          URL: ${{ github.event.pull_request.html_url }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          CONTENT=$(printf '🎉 **PR MERGIATO** — #%s\n**%s**\n%s' "$NUM" "$TITLE" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"

      - name: Issue closed
        if: github.event_name == 'issues' && github.event.action == 'closed'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NUM: ${{ github.event.issue.number }}
          TITLE: ${{ github.event.issue.title }}
          URL: ${{ github.event.issue.html_url }}
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then echo "no webhook, skip"; exit 0; fi
          CONTENT=$(printf '✅ **Issue chiusa** — #%s\n%s\n%s' "$NUM" "$TITLE" "$URL")
          PAYLOAD=$(jq -n --arg c "$CONTENT" '{content: $c}')
          curl -sS -X POST -H 'Content-Type: application/json' -d "$PAYLOAD" "$DISCORD_WEBHOOK"
